# Bright Data — MCP server + API REST
BRIGHT_DATA_API_TOKEN=

# Apify — MCP server + API REST
APIFY_TOKEN=

# Anthropic — LLM fallback (pass 4)
ANTHROPIC_API_KEY=

# Browser Use Cloud — agent browser IA (pass 3)
BROWSER_USE_API_KEY=
